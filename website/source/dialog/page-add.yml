title:
  en: Add {{viewData.title}}
  gr: Προσθήκη {{viewData.title}}

authorization:
  - editor

scripts:
  - "lib/string.min"

controller: AgPageAdd

form:
  name: "formSubmit"
  onSubmit: "submit()"
  fields:
    - title:
        en: "Template"
      type: "text"
      name: template
      group: "input"
      validators:
        required: true
