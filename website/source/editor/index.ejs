<div layout="column"
  class="editor-container">
  <%-await template('./toolbar')%>
  <div ag-include
    class="page-main"
    ng-class="{active:page}"
    page="{{mode.page}}"></div>
</div>
<%-await template('editor/messages')%>
<%-await template('editor/field-menu')%>