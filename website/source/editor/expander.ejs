<%
  var ngIf = '';
  if(locals.if){
    ngIf = `ng-if="${locals.if}"`;
  }
%>
<md-button <%-ngIf%> ng-click="toggleView(data)" class="expander md-icon-button" aria-label="expand">
  <md-icon md-svg-icon="<%-get_icon('material/keyboard-arrow-down')%>">
  </md-icon>
</md-button>