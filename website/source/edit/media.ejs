<%
  var pages = ['edit/media/public','edit/media/private'];
  for(var page of pages){
    page = await get_page(page);
    %>
<md-button ng-href="<%-await get_path(page)%>">
  <%-lng(page.title)%>
</md-button>
<%
  }
%>